# Copiá este archivo a ".env.local" (queda fuera de Git, nunca se sube) y
# pegá ahí tus valores reales para probar en tu computadora con `vercel dev`.
# En Vercel estas mismas variables se cargan en Project Settings → Environment
# Variables, no acá.

# Access Token de Mercado Pago (el que empieza con TEST- para pruebas,
# APP_USR- cuando ya sea de verdad). Se copia desde
# mercadopago.com.ar/developers/panel → tu aplicación → Credenciales.
# ¡Nunca el mismo valor en TEST y en producción!
MP_ACCESS_TOKEN=

# Opcional pero recomendado: la "Clave secreta" que Mercado Pago muestra al
# configurar el webhook (Tu aplicación → Webhooks → Configurar notificaciones).
# Si se deja vacío, el webhook igual funciona (valida el pago llamando
# directo a la API de Mercado Pago), pero no puede confirmar que la
# notificación vino realmente de Mercado Pago antes de mirarla.
MP_WEBHOOK_SECRET=

# Connection string de la base Postgres en Neon (neon.tech). Ahí se guardan
# las noches que ya se bloquearon online (pagadas o pendientes de WhatsApp).
# Si la conectás desde Storage → Connect Store en el panel de Vercel, Vercel
# la carga sola con este mismo nombre; si la creaste directo en Neon, se
# copia desde su panel (Connection Details).
DATABASE_URL=

# Clave para el panel nuevo de admin.html (ver, confirmar y dar de baja las
# reservas que bloquearon fecha en la base). Inventá cualquier texto largo;
# es lo único que impide que esa URL quede abierta a cualquiera. Sin esto
# cargado, ese panel de admin.html no funciona — falla cerrado.
ADMIN_TOKEN=
