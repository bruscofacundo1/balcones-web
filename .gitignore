# archivos temporales de Office / Windows
~$*
Thumbs.db
desktop.ini
.DS_Store

# material original de trabajo (queda fuera del repo por peso)
*.zip
*.mp4
*.MOV

# claves: el Access Token de Mercado Pago vive acá para probar en la compu.
# NUNCA se sube — en Vercel se carga aparte, como variable de entorno.
.env
.env.local
.env*.local

# dependencias de las funciones serverless (api/) y carpeta de Vercel
node_modules/
.vercel/
