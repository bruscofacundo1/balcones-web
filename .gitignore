# archivos temporales de Office / Windows
~$*
Thumbs.db
desktop.ini
.DS_Store

# material original de trabajo (queda fuera del repo por peso)
*.zip
*.mp4
*.MOV

# claves: el Access Token de Mercado Pago vive acá para probar en la compu.
# NUNCA se sube — en Cloudflare se carga aparte, como variable de entorno.
.env
.env.local
.env*.local
.dev.vars

# dependencias de las funciones (functions/, lib/) y carpetas locales de build
node_modules/
.vercel/
.wrangler/
